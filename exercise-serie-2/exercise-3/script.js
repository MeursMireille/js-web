//Check to see if script is linked properly.
console.log("This script is linked properly!")


//Write your JS code here...
let count = 0;
let result = i; 
/* const transforms = ['scale(.5)', 'scale(5)']; */


function add() {
    count = count + 1;
    document.getElementById('text').value = count;
  }
  add();

/* for (let click =+ 1; transforms++ ) {
  const transforms = array[0,1];
  return click;
} */
/* function multiplier() {
  
}


  while (i=0, i<49, i++) {
    return i();
  }

  while (i=0, i>49, i+2) {
    return i();
  }
   */